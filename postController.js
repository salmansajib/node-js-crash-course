const posts = [
  { id: 1, title: 'post one' },
  { id: 2, title: 'post two' },
];

const getPosts = () => posts;

export const getPostLength = () => posts.length;

// export { getPosts };

export default getPosts;
